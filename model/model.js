const modelContact = require('./contact')
const modelGroup = require('./group')
const modelGroupContact = require('./contact-group')

module.exports = {
    modelContact: modelContact,
    modelGroup: modelGroup,
    modelGroupContact: modelGroupContact
}